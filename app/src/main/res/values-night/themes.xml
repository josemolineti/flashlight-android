<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Flashlight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/first_dark</item>
        <item name="colorSecondary">@color/second_dark</item>
        <item name="colorTertiary">@color/third_dark</item>
    </style>

    <!-- Dark mode -->
    <style name="BACKGROUND">
        <item name="background">@color/white</item>
    </style>
    <style name="TEXT">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="BUTTON">
        <item name="cornerRadius">12dp</item>
        <item name="android:src">@drawable/flashlight_button_shape</item>
    </style>
    <style name="BUTTON_PRESSED">
        <item name="android:src">@drawable/flashlight_button_shape_press</item>
    </style>
</resources>